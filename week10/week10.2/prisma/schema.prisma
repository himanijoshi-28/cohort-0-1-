generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://joshihimani2507:G48pSPEjBrhn@ep-fancy-cell-61357429.us-east-2.aws.neon.tech/week10.2?sslmode=require"
}

model User {
  Id        Int     @id @default(autoincrement())
  firstname String?
  lastname  String?
  password  String
  email     String  @unique
}

model Todo {
  id          Int     @id @default(autoincrement())
  title       String
  description String?
  done        Boolean @default(false)
  userId      Int
}
